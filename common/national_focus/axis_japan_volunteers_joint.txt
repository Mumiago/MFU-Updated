### Axis satellite joint branch: authorize volunteer deployments to Japan (sets Vols_japan_allowed).

joint_focus = {
	id = AXIS_authorize_volunteers_to_japan
	icon = GFX_jap_rocket

	# Grid (0, 0) on every tree; per-tag offset blocks keep parser/tag routing explicit.
	x = 0
	y = 0

	offset = {
		x = 0
		y = 0
		trigger = { tag = HUN }
	}

	offset = {
		x = 0
		y = 0
		trigger = { tag = SLO }
	}

	offset = {
		x = -4
		y = 0
		trigger = { tag = ROM }
	}

	offset = {
		x = 0
		y = 0
		trigger = { tag = BUL }
	}

	offset = {
		x = -4
		y = 0
		trigger = { tag = SPR }
	}

	cost = 5

	allow_branch = {
		OR = {
			tag = HUN
			tag = SLO
			tag = ROM
			tag = BUL
			tag = SPR
		}
	}

	cancel_if_invalid = yes
	continue_if_invalid = no
	available_if_capitulated = yes

	ai_will_do = { factor = 1 }

	available = {
		country_exists = JAP
		CHI = { has_war = yes }
	}

	bypass = {
		OR = {
			GER = { has_war_with = POL }
		}
	}

	completion_reward_joint_originator = {
		custom_effect_tooltip = AXIS_authorize_volunteers_to_japan_tt
		hidden_effect = {
			MFU_recall_all_volunteers_and_forbid_spain_volunteers = yes
			set_country_flag = Vols_japan_allowed
			add_ideas = AXIS_abroad_volunteers_japan_expedition
		}

	}
}
