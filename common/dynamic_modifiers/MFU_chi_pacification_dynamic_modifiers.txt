CHI_pacified_by_japan = { #STATE MODIFIER
	enable = { always = yes }
	icon = GFX_modifiers_JAP_army_control

	attacker_modifier = yes

	remove_trigger = { # ROOT refers to the state on which the modifier is added
		ROOT = {
			OR = {
				compliance < 50
				has_state_flag = CHI_assassinations_cleared_flag
				NOT = { is_core_of = CHI }
				NOT = {
					OR = {
						is_controlled_by = JAP
						is_controlled_by = MAN
						is_controlled_by = RCC
					}
				}
			}
		}
	}

	army_core_attack_factor = -0.3
	army_core_defence_factor = -0.3
}
