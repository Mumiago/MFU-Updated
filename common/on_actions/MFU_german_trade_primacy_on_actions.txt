# The Axis is a fixed roster in this mod, so this is a one-shot list rather than a
# faction sweep. GER is the buyer and is deliberately absent.
# SLO and SPR own territory from day one in MFU. EFR does not exist until Vichy is
# released; ideas and opinion applied to a tag beforehand persist to it, the same way
# EFR already carries ROM_ger_trade_treaty from its own history file.
on_actions = {

	on_startup = {
		effect = {
			ITA = { MFU_grant_german_trade_primacy = yes }
			HUN = { MFU_grant_german_trade_primacy = yes }
			ROM = { MFU_grant_german_trade_primacy = yes }
			BUL = { MFU_grant_german_trade_primacy = yes }
			SLO = { MFU_grant_german_trade_primacy = yes }
			SPR = { MFU_grant_german_trade_primacy = yes }
			EFR = { MFU_grant_german_trade_primacy = yes }
		}
	}
}
