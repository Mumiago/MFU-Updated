SOV_barbarossa = {
	allowed = { 
		tag = SOV 
	}
	
	enable = {
		has_war_with = GER
	}

	abort = {
		date > 1941.11.10
	}

	ai_strategy = {
		type = front_control
		tag = GER 
		ordertype = front
		execution_type = careful
		manual_attack = no 
		execute_order = no
	}
}


GER_barbarossa = {
	allowed = { 
		tag = GER 
	}
	
	enable = {
		has_war_with = SOV
	}

	abort = {
		date > 1941.11.15
	}

	ai_strategy = {
		type = front_control
		tag = SOV 
		ordertype = front
		execution_type = rush 
		manual_attack = yes
		execute_order = yes
	}
}


#################### GiGa Monkee Railroad ####################

GER_soviet_counter_offensive = {
	allowed = { 
		tag = GER 
	}
	
	enable = {
		date > 1941.11.16
	}

	abort = {
		date > 1942.4.15
	}

	ai_strategy = {
		type = front_control
		tag = SOV 
		ordertype = front
		execution_type = careful
		manual_attack = no 
		execute_order = no
	}
}

GER_fall_blau = {
	allowed = { 
		tag = GER 
	}
	
	enable = {
		has_war_with = SOV
		#date > 1942.5.1
		has_idea = GER_blau_I
	}

	abort = {
		#date > 1942.9.15
		NOT = { has_idea = GER_blau_I }
	}

	ai_strategy = {
		type = front_control
		tag = SOV 
		ordertype = front
		execution_type = rush 
		manual_attack = yes
		execute_order = yes
	}
}

GER_die_ostfront = {
	allowed = { 
		tag = GER 
	}
	
	enable = {
		has_war_with = SOV
		date > 1942.9.16
		NOT = { has_idea = GER_blau_I }
	}

	abort = {
		OR = {
			has_idea = GER_blau_I
			date > 1943.5.1
		}
	}

	ai_strategy = {
		type = front_control
		tag = SOV 
		ordertype = front
		execution_type = careful
		manual_attack = no 
		execute_order = no
	}
}

GER_citadel = {
	allowed = { 
		tag = GER 
	}
	
	enable = {
		has_war_with = SOV
		OR = {
			has_idea = GER_blau_I
			date > 1943.5.2
		}

	}

	abort = {
		OR = {
			date > 1943.9.15
			has_manpower < 100000
		}
	}

	ai_strategy = {
		type = front_control
		tag = SOV 
		ordertype = front
		execution_type = rush 
		manual_attack = yes
		execute_order = yes
	}
}


SOV_winter_offensive = {
	allowed = { 
		tag = SOV 
	}

	enable = {
		has_idea = winter_offensive
	}

	abort = {	
		NOT = { has_idea = winter_offensive }
	}

	ai_strategy = {
		type = front_control
		tag = GER 
		ordertype = front
		execution_type = balanced 
		manual_attack = yes
		execute_order = yes
	}
}

SOV_dont_waste_resources = {
	allowed = { 
		tag = SOV 
	}
	
	enable = {
		date > 1942.2.20
	}

	abort = {
		date > 1942.9.11
	}

	ai_strategy = {
		type = front_control
		tag = GER 
		ordertype = front
		execution_type = careful
		manual_attack = no 
		execute_order = no
	}
}

SOV_end_reich = {
	allowed = { 
		tag = SOV 
	}

	enable = {
		date > 1942.9.12
		has_idea = winter_offensive
	}

	abort = {
		NOT = { has_idea = winter_offensive }
	}

	ai_strategy = {
		type = front_control
		tag = GER 
		ordertype = front
		execution_type = balanced
		manual_attack = yes
		execute_order = yes
	}
}