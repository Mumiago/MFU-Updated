special_forces_doctrine = {
    folder = special_forces

    name = GRAND_DOCTRINE_SPECIAL_FORCES_QUANTITY
    description = GRAND_DOCTRINE_SPECIAL_FORCES_QUANTITY_DESC
    icon = GFX_doctrine_commandos_medium
    available = {

    }

    xp_cost = 0
    xp_type = army

    ai_will_do = {
        base = 1
    }

    tracks = {
        sf_first_track
        sf_second_track
    }
    max_track_rows = 1
    max_track_columns = 2

    milestones = {
        {
            special_forces_cap = 0.04
            category_special_forces = {
                soft_attack = 0.05
            }
            effect = {
                if = {
                    limit = { NOT = { has_variable = special_forces_milestone_var } }
                    set_variable = { special_forces_milestone_var = 1 }
                }
                else = { add_to_variable = { special_forces_milestone_var = 1 } }
            }
        }
        {
            special_forces_cap = 0.04
            category_special_forces = {
                max_organisation = 5
            }
            effect = {
                if = {
                    limit = { NOT = { has_variable = special_forces_milestone_var } }
                    set_variable = { special_forces_milestone_var = 1 }
                }
                else = { add_to_variable = { special_forces_milestone_var = 1 } }
            }
        }
    }

    # EFFECTS
    category_special_forces = {
        max_organisation = 5
    }
}
