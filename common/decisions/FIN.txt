war_measures = {
	FIN_surrender_to_soviets = {
		icon = generic_political_reform
		available = {
			date < 1941.1.1
			has_war_with = SOV
		}
		visible = {
			original_tag = FIN
		}
		cost = 0
		days_remove = 7
		fire_only_once = yes
		complete_effect = {
			set_country_flag = FIN_Surrender
		}
	}

FIN_quick_end_to_winter_war = {
	icon = generic_attack
	days_mission_timeout = 200
	fire_only_once = yes

	activation = {
		has_war_with = SOV
		date < 1941.1.1
	}

	visible = {
		original_tag = FIN
		has_war_with = SOV
		date < 1941.1.1
	}

	complete = {
		OR = {
			}
	}
	complete_effect = {
		set_country_flag = FIN_quick_end_winter_war
		SOV = { 
				set_country_flag = SOV_winter_war_mission_over
			}
			FIN = {
				every_unit_leader = {
					remove_unit_leader_trait = Winter_War_Grind
				}
			}
			FIN = { country_event = finland.9 }
			hidden_effect = {
				FIN = {
					set_technology = { FIN_winter_war_over = 1 }
				    set_country_flag = FIN_won_winter_war
				}				
			}
			FIN = { country_event = finland.7 }
	}
	timeout_effect = {
	}
}
}