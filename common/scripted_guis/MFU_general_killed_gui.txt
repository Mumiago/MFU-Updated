scripted_gui = {

	# Shown to the country that LOST the general (flag set in mfu_general_death.1).
	custom_general_killed_warning = {
		context_type = player_context
		window_name = "custom_general_killed_warning"

		visible = {
			has_country_flag = ui_show_general_killed
		}

		effects = {
			ok_button_click = {
				clr_country_flag = ui_show_general_killed
			}
		}

		properties = {
			sender_flag = {
				image = "[?gk_other.GetFlag]"
			}
			receiver_flag = {
				image = "[This.GetFlag]"
			}
		}
	}

	# Shown to the country that KILLED the enemy general.
	custom_enemy_general_killed = {
		context_type = player_context
		window_name = "custom_enemy_general_killed"

		visible = {
			has_country_flag = ui_show_enemy_general_killed
		}

		effects = {
			ok_button_click = {
				clr_country_flag = ui_show_enemy_general_killed
			}
		}

		properties = {
			sender_flag = {
				image = "[?gk_other.GetFlag]"
			}
			receiver_flag = {
				image = "[This.GetFlag]"
			}
		}
	}

}
