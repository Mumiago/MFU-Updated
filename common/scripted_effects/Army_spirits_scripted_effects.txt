update_academy_weekly = {
	# Called from on_weekly, which fires once per country (ROOT = that country).
	# Operate on ROOT directly. Do NOT wrap in every_country: that would make every
	# country's weekly tick add the increment to every other country, multiplying it
	# by the (constantly changing) number of living countries.
	if = {
		limit = { has_idea = political_loyalty_spirit }
		add_to_variable = { academy_weekly_pp = 0.003 }
	}
	if = {
		limit = { has_idea = professional_officer_corps_spirit }
		add_to_variable = { academy_special_forces_capacity = 0.35 }
	}
	if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			NOT = {
				tag = JAP 
				tag = SOV 
				tag = ITA 
				tag = GER
				tag = USA 
				tag = MAN 
				tag = CHI 
			}
		}
		add_to_variable = { academy_experience_gain = 0.0015 }
	}


	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = JAP 
			NOT = {
				has_idea = jap_kwantung_army_4
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 1.5385
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = JAP 
			has_idea = jap_kwantung_army_4
			
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 20
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			OR = {
				tag = ITA 
			}
		}
		add_to_variable = { academy_experience_gain = 0.00215 }
	}

	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			OR = {
				tag = GER 
			}
		}
		add_to_variable = { academy_experience_gain = 0.00273 }
	}

	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = SOV 
			has_idea = SOV_STAVKA1
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 2
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = SOV 
			OR = {	
				has_idea = SOV_STAVKA2
				has_idea = SOV_STAVKA_2
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 1.8185
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = SOV 
			OR = {	
				has_idea = SOV_STAVKA_3
				has_idea = SOV_STAVKA3
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 1.6668
		}
	}

	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = CHI 
			CHI = {
				NOT = {
					has_war_with = JAP 
				}
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 2
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = CHI 
			CHI = {
				has_war_with = JAP 
			}
			USA = {
				NOT = {
					has_war_with = JAP
				}
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 6.67
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = CHI 
			CHI = {
				has_war_with = JAP 
			}
			USA = {
				has_war_with = JAP
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 2.223
		}
	}

	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = USA 
			USA = {
				check_variable = { USA_experience_gain_army_factor_var = -0.50 }
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 2
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = USA 
			USA = {
				check_variable = { USA_experience_gain_army_factor_var = -0.40 }
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 1.667
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = USA 
			USA = {
				check_variable = { USA_experience_gain_army_factor_var = -0.35 }
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 1.5385
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = USA 
			USA = {
				check_variable = { USA_experience_gain_army_factor_var = -0.30 }
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 1.429
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = USA 
			USA = {
				check_variable = { USA_experience_gain_army_factor_var = -0.25 }
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 1.334
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = MAN
			MAN = {
				has_idea = jap_kwantung_army_3
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 2.858
		}
	}
	else_if = {
		limit = { 
			has_idea = relief_of_command_spirit 
			tag = MAN
			MAN = {
				NOT = {
					has_idea = jap_kwantung_army_3
				}
				
			}
		}
		add_to_variable = { academy_experience_gain_japan_dummy = 0.0015 }
		set_variable = { academy_experience_gain = 0 }
		add_to_variable = { academy_experience_gain = academy_experience_gain_japan_dummy }
		multiply_variable = {
			var = academy_experience_gain
			value = 1
		}
	}

   
}
